#include <stdio.h>
#include <math.h>

int main() {
    printf("sqrt(16) = %.2f\n", sqrt(16));
    printf("pow(2, 3) = %.2f\n", pow(2, 3));
    printf("sin(0) = %.2f\n", sin(0));
    printf("cos(0) = %.2f\n", cos(0));
    printf("ceil(3.2) = %.2f\n", ceil(3.2));
    return 0;
}
